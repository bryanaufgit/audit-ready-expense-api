# Scripts

This folder contains helper scripts for reproducible local execution.

Planned scripts:
- `run-api.sh`            Start API (with environment flags)
- `run-robot.sh`          Run Robot regression tests
- `reset-db.sh`           Reset local DB (dev only)

All scripts should be idempotent and safe to re-run.